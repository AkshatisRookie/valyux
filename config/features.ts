/**
 * Feature flags — flip to true when Electronics / Flights are ready to launch.
 * Navbar and App.tsx use these so full page code stays imported but gated.
 */
export const FEATURE_ELECTRONICS_PAGE = false;
export const FEATURE_FLIGHTS_PAGE = false;
